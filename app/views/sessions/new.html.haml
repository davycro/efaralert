.container
  .login-container
    .row
      .span6
        %h2 Notice
        %p
          The dispatch software will be offline this week for upgrades. We will launch again with a better program on May 6.
          %br
          %br
          %em Thank you,
          %br
          %em David Crockett and McKenzie Wilson

.container.hide
  .login-container
    .row
      .span2
        = image_tag 'efar_login_logo_med.png'
      .span6
        %form.form-horizontal{ :action => session_path, :method => "POST" }
          %legend Dispatch Panel
          
          - if flash[:notice].present?
            .alert.alert-info=flash[:notice]
          - if flash[:alert].present?
            .alert=flash[:alert]

          %p Content restricted to ambulance dispatchers, please login for access:

          .control-group
            %label.control-label Name
            .controls
              = select "dispatcher", "id", Dispatcher.all.collect { |r| [r.full_name, r.id] }

          .control-group
            %label.control-label Drainage
            .controls
              = select "drainage", "id", Suburb.all.collect { |r| [r.name, r.id] }

          .control-group
            %label.control-label Password
            .controls
              %input{:type=>"password", :name=>"password", :placeholder=>"Password"}

          .form-actions
            %button.btn{:type=>"submit"} Sign In
          