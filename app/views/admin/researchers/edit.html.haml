- content_for :section_header do
  %h3 Edit researcher

- content_for :section_actions do
  %a.btn.btn-link{:href => admin_researchers_path } Cancel

- if @researcher.errors.present?
  .alert.alert-error There were some errors updating the researcher. See below for more information.

= render :partial => 'editor'