= form_for([:admin, @dispatcher], :html => {:class => 'form-horizontal'}) do |f|
  
  %legend
    Dispatcher

  - if @dispatcher.errors.present?
    .alert.alert-error There were some errors saving the dispatcher. See below for more information.

  = horizontal_text_field(@dispatcher, :full_name)
  = horizontal_text_field(@dispatcher, :username)
  = horizontal_password_field(@dispatcher)

  .form-actions
    %button.btn.btn-primary{:type=>'submit'} Save
    %a.btn{:href=>admin_dispatchers_path} Cancel
